"""Placeholder — the old tools have been removed.

The original `challenge.tools.csv_reader` and `challenge.tools.pdf_report`
were tied to the smolagents agent and have been replaced by the typed
pipeline (`data_io.py`, `joins.py`, `summary.py`, `render.py`). New tests
live in `test_summary.py`, `test_verify.py`, and `test_pipeline.py`.
"""
